# Expo / React Native peer-dep matrix is wide; legacy-peer-deps keeps the monorepo installable
# with plain `npm install`. In the mobile app, prefer `npx expo install <pkg>` so versions match
# the Expo SDK. This does NOT affect apps/api (the tested core), which has clean peers.
legacy-peer-deps=true
