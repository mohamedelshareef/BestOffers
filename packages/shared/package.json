{
  "name": "@bestoffers/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types & contracts across api / mobile / admin (per system-design.md).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
